{
  "title": "Universal Lead Generator Input",
  "description": "Generate leads for any industry and any persona from Google Maps, LinkedIn, business directories, and general web search.",
  "type": "object",
  "schemaVersion": 1,
  "properties": {
    "icpDescription": {
      "title": "Ideal Customer Profile (ICP)",
      "type": "string",
      "description": "Free text describing who you're targeting: industry, persona/job titles, company size, region, pain points, etc. Used by Claude to score every lead's fit.",
      "editor": "textarea",
      "prefill": "UK-based independent dental clinics with 5-30 staff. Best contacts are the Practice Manager or Owner. Looking for practices without an online booking system."
    },
    "searchQueries": {
      "title": "Local business search terms",
      "type": "array",
      "description": "Business types to look up in the local business source (OpenStreetMap), e.g. [\"dental clinics\", \"dentists\"]. A `location` is required for this source. Leave empty to skip it.",
      "editor": "stringList",
      "prefill": ["dental clinics"]
    },
    "keywords": {
      "title": "Keywords (LinkedIn / directory / web search)",
      "type": "array",
      "description": "Industry/company keywords used to build LinkedIn, directory, and web-search queries. Defaults to searchQueries if left empty.",
      "editor": "stringList"
    },
    "personaTitles": {
      "title": "Persona job titles (LinkedIn)",
      "type": "array",
      "description": "Job titles to search for on LinkedIn, e.g. [\"Practice Manager\", \"Owner\"]. Defaults to a generic owner/founder/director list.",
      "editor": "stringList"
    },
    "location": {
      "title": "Location",
      "type": "string",
      "description": "City, region, or country to target, e.g. \"London, UK\". Used across all sources.",
      "editor": "textfield",
      "prefill": "London, UK"
    },
    "countryCode": {
      "title": "Proxy country code",
      "type": "string",
      "description": "2-letter country code for residential proxy routing, e.g. GB, US. Leave empty for no country restriction.",
      "editor": "textfield",
      "prefill": "GB"
    },
    "directoryUrls": {
      "title": "Directory URLs (optional)",
      "type": "array",
      "description": "Specific business-directory search/listing URLs to crawl (Yell, Thomson Local, etc). If empty and the directory source is enabled, a Yell search is built automatically from keywords + location.",
      "editor": "stringList"
    },
    "sources": {
      "title": "Lead sources to run",
      "type": "array",
      "description": "Which sources to run. Defaults to all four. (The legacy id \"googleMaps\" is still accepted and maps to \"localBusiness\".)",
      "editor": "select",
      "items": {
        "type": "string",
        "enum": ["localBusiness", "linkedin", "directory", "webSearch"],
        "enumTitles": ["Local business (OpenStreetMap)", "LinkedIn", "Business directories", "General web search"]
      },
      "default": ["localBusiness", "linkedin", "directory", "webSearch"]
    },
    "maxResultsPerSource": {
      "title": "Max results per source",
      "type": "integer",
      "description": "Cap on how many leads each individual source (per query) can return.",
      "default": 50,
      "minimum": 1,
      "maximum": 1000
    },
    "minScoreThreshold": {
      "title": "Minimum ICP score threshold",
      "type": "integer",
      "description": "Only export leads scoring at or above this value (0-100).",
      "default": 0,
      "minimum": 0,
      "maximum": 100
    },
    "enrichWebsites": {
      "title": "Enrich leads from their websites",
      "type": "boolean",
      "description": "Visit each lead's website (and contact page) to extract emails, phone numbers, and social links.",
      "default": true
    },
    "fetchLinkedInPublicProfiles": {
      "title": "Fetch LinkedIn public profile pages",
      "type": "boolean",
      "description": "Best-effort fetch of each discovered LinkedIn profile's public page for extra detail. Uses no cookies/login; some profiles may be behind a login wall and will keep only the search-snippet data.",
      "default": true
    },
    "useApifyActors": {
      "title": "Use external Apify Actors (paid) for richer data",
      "type": "boolean",
      "description": "When ON, the local-business, LinkedIn, and directory sources first try a proven external Apify Actor (Google Maps, LinkedIn, directory) and only fall back to the built-in crawlers if those can't run. ⚠️ Requires an Apify plan that permits running public Actors, and those Actors bill you separately (a few cents per result). On a plan that can't run public Actors, leave this OFF — the built-in crawlers are used instead.",
      "default": false
    },
    "mapsActorId": {
      "title": "Google Maps Actor id",
      "type": "string",
      "description": "External Apify Actor used for local businesses when 'Use external Apify Actors' is ON. Leave default unless you prefer another Google Maps Actor.",
      "editor": "textfield",
      "default": "compass/crawler-google-places"
    },
    "linkedinActorId": {
      "title": "LinkedIn Actor id",
      "type": "string",
      "description": "External Apify Actor used for LinkedIn people search when 'Use external Apify Actors' is ON.",
      "editor": "textfield",
      "default": "harvestapi/linkedin-profile-search"
    },
    "directoryActorId": {
      "title": "Directory Actor id (optional)",
      "type": "string",
      "description": "Optional external Apify Actor for business directories when 'Use external Apify Actors' is ON. Leave empty to always use the built-in directory crawler.",
      "editor": "textfield",
      "default": ""
    },
    "groqApiKey": {
      "title": "Groq API key",
      "type": "string",
      "description": "Used to score every lead against your ICP via Groq's hosted LLM API. If omitted, a simple rule-based fallback score is used instead.",
      "editor": "textfield",
      "isSecret": true
    },
    "groqModel": {
      "title": "Groq model",
      "type": "string",
      "description": "Groq model used for lead scoring.",
      "editor": "textfield",
      "default": "llama-3.3-70b-versatile"
    },
    "outputFormat": {
      "title": "Output format",
      "type": "string",
      "description": "Choose whether to output JSON, CSV, or both to the key-value store (the Apify Dataset always gets the structured records).",
      "enum": ["json", "csv", "both"],
      "default": "both"
    }
  },
  "required": ["icpDescription"]
}
